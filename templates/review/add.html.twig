{% extends 'base.html.twig' %}

{% block title %}Ajouter une critique{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col">
            <h1>Ajouter une critique</h1>
            <h2>{{ movie.title }}</h2>

            {# On peut décomposer le rendu du form #}

            {# Ouverture de balise form #}
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                {# Affiche tous les champs du formulaire #}
                {{ form_widget(form) }}
                {# On ajoute manuellement le bouton submit #}
                <button type="submit" class="btn btn-success">Ajouter</button>
            {# Fermeture du formulaire #}
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}